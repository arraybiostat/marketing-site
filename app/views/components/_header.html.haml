- content_for :logo do
  %img{ :src => "/images/array-logo.gif", :alt => "Array Biostatistics" }

= current_or_link('', yield(:logo))

- content_for :contact do
  = current_or_link('contact', '<strong>CONTACT</strong>')

- content_for :services do
  = current_or_link('services', '<strong>SERVICES</strong>')

- content_for :who do
  = current_or_link('who', '<strong>WHO WE ARE</strong>')
  = ":"
  = current_or_link('customer', 'customer service model')
  = ":"
  = current_or_link('leadership', 'project leadership strategy')

- content_for :home do
  = current_or_link('', 'HOME')


%ul.unstyled.navigation
  - [ :contact, :services, :who, :home ].each do |bracket_content|
    %li
      %span.bracket [
      = yield(bracket_content)
      %span.bracket ]
