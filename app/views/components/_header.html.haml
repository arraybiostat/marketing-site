- content_for :logo do
  %img{ :src => "/images/array-logo.gif", :alt => "Array Biostatistics" }

= current_or_link('', yield(:logo))

- content_for :jobs do
  %a{ href: 'https://docs.google.com/document/d/1JzKJVp33hyHBvV09Tr0faJ0WP9H9rrywKd4Zzin5ods/pub'} JOBS
  
- content_for :contact do
  = current_or_link('contact', '<strong>CONTACT</strong>')

- content_for :services do
  = current_or_link('services', '<strong>SERVICES</strong>')

- content_for :who do
  = current_or_link('who_we_are', '<strong>WHO WE ARE</strong>')
  = ":"
  = current_or_link('customer_service_model', 'customer service model')
  = ":"
  = current_or_link('project_leadership_strategy', 'project leadership strategy')


%ul.unstyled.navigation
  - [ :contact, :services, :jobs, :who ].each do |bracket_content|
    %li
      %span.bracket [
      = yield(bracket_content)
      %span.bracket ]
